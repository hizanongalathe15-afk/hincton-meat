{
  "name": "hincton-monorepo",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "vercel-build": "npm --prefix frontend run build && npm --prefix backend run vercel-build"
  },
  "dependencies": {
    "serverless-http": "^3.0.0"
  }
}
