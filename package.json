{
  "name": "hincton-monorepo",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "start": "npm --prefix backend start",
    "build": "npm --prefix backend run render-build",
    "render-build": "npm --prefix backend run render-build",
    "db:push": "npm --prefix backend run db:push"
  },
  "dependencies": {
    "serverless-http": "^3.0.0"
  }
}
